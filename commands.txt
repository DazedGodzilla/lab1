dir
mkdir lab1
cd lab1
git init

git config --global user.name "DazedGodzilla"
git config --global user.email nene.muse@gmail.com
nano
git add l1.txt
git status
git commit -m "Initial commit"
nano l1.txt
git commit -a -m "modify l1.txt"
$ git remote add origin https://github.com/DazedGodzilla/lab1.git
$ git push -u origin master
nano l1.txt
$ git reset --hard HEAD
$ git branch logging
$ git checkout logging
git log
nano 
git add log.txt
$ git commit -m "Added log"
git push --set-upstream origin logging
git checkout master
ls
git merge logging
git push

git branch allcomands
git checkout allcomands
nano
git add commands.txt
git commit -m "added commands"
git push --set-upstream origin allcomands

